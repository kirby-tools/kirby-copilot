import { defineConfig } from "vitepress";

// https://vitepress.dev/reference/site-config
export default defineConfig({
  title: "Kirby Copilot",
  description: "Your AI companion for the Kirby Panel.",
  appearance: "force-dark",
  themeConfig: {
    aside: false,
  },
});
